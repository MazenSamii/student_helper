import 'package:flutter/cupertino.dart';

class CSmaterials extends StatelessWidget {
  const CSmaterials({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
