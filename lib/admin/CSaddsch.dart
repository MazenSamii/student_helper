import 'package:flutter/cupertino.dart';

class CSaddsch extends StatelessWidget {
  const CSaddsch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
