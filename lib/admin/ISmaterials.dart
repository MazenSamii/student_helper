import 'package:flutter/cupertino.dart';

class ISmaterials extends StatelessWidget {
  const ISmaterials({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
