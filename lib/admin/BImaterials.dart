import 'package:flutter/cupertino.dart';

class BImaterials extends StatelessWidget {
  const BImaterials({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
