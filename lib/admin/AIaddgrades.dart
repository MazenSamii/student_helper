import 'package:flutter/cupertino.dart';

class AIaddgrades extends StatelessWidget {
  const AIaddgrades({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
