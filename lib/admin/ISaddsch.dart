import 'package:flutter/cupertino.dart';

class ISaddsch extends StatelessWidget {
  const ISaddsch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
