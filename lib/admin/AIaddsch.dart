import 'package:flutter/cupertino.dart';

class AIaddsch extends StatelessWidget {
  const AIaddsch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
