import 'package:flutter/cupertino.dart';

class ISaddgrades extends StatelessWidget {
  const ISaddgrades({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
