import 'package:flutter/cupertino.dart';

class BIaddsch extends StatelessWidget {
  const BIaddsch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
