import 'package:flutter/cupertino.dart';
class AImaterials extends StatelessWidget {
  const AImaterials({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
