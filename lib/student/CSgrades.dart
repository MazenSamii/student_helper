import 'package:flutter/cupertino.dart';

class CSgrades extends StatelessWidget {
  const CSgrades({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
