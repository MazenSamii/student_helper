import 'package:flutter/cupertino.dart';

class BIsch extends StatelessWidget {
  const BIsch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
