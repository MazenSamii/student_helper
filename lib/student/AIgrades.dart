import 'package:flutter/cupertino.dart';

class AIgrades extends StatelessWidget {
  const AIgrades({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
