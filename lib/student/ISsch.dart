import 'package:flutter/cupertino.dart';

class ISsch extends StatelessWidget {
  const ISsch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
