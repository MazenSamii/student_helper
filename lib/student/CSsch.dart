import 'package:flutter/cupertino.dart';

class CSsch extends StatelessWidget {
  const CSsch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
