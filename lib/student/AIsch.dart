import 'package:flutter/cupertino.dart';

class AIsch extends StatelessWidget {
  const AIsch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
